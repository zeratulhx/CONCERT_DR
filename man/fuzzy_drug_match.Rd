% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result_summary.R
\name{fuzzy_drug_match}
\alias{fuzzy_drug_match}
\title{Fuzzy String Matching for Drug Names}
\usage{
fuzzy_drug_match(
  query_names,
  reference_names,
  method = "levenshtein",
  threshold = 80,
  top_n = 1
)
}
\arguments{
\item{query_names}{Vector of query drug names}

\item{reference_names}{Vector of reference drug names to match against}

\item{method}{Similarity method: "levenshtein" (default), "jaro", or "jarowinkler"}

\item{threshold}{Minimum similarity threshold (0-100)}

\item{top_n}{Number of top matches to return for each query (default: 1)}
}
\value{
Data frame with query names, matched names, and similarity scores
}
\description{
Performs fuzzy string matching to find the best matches between query drug names
and a reference list of drug names. Uses Levenshtein distance for similarity calculation.
}
\examples{
\dontrun{
query <- c("aspirin", "ibuprofen", "acetaminophen")
reference <- c("aspirin", "ibuprofen", "acetaminophen", "naproxen", "diclofenac")

matches <- fuzzy_drug_match(query, reference, threshold = 80)
print(matches)
}

}
