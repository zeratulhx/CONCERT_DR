% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/result_summary.R
\name{annotate_drug_results}
\alias{annotate_drug_results}
\title{Annotate CONCERTDR Results with Drug Information}
\usage{
annotate_drug_results(
  results_df,
  sig_info_file,
  comp_info_file,
  drug_info_file,
  output_file = NULL,
  fuzzy_threshold = 90,
  perfect_match_only = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{results_df}{Data frame containing signature matching results with a 'compound' column}

\item{sig_info_file}{Path to siginfo_beta.txt file or data frame with signature information}

\item{comp_info_file}{Path to compound information file or data frame}

\item{drug_info_file}{Path to drug information file or data frame with clinical phase data}

\item{output_file}{Optional path to save annotated results (default: NULL, no file saved)}

\item{fuzzy_threshold}{Minimum similarity score for fuzzy matching (default: 90)}

\item{perfect_match_only}{Logical; if TRUE, only accept perfect matches (score=100) for fuzzy matching (default: FALSE)}

\item{verbose}{Logical; whether to print progress messages (default: TRUE)}
}
\value{
Data frame with annotated results including drug names, aliases, MOA, and clinical phases
}
\description{
This function annotates signature matching results from CONCERTDR with drug
information including compound names, aliases, mechanism of action (MOA),
and clinical phase data. It performs fuzzy string matching to find the best
matches when exact matches are not available.
}
\examples{
\dontrun{
# Read signature matching results
results <- read.csv("sig_match_xsum_results.csv")

# Annotate with drug information
annotated_results <- annotate_drug_results(
  results_df = results,
  sig_info_file = "siginfo_beta.txt",
  comp_info_file = "compound_info.txt",
  drug_info_file = "drug_info.txt",
  output_file = "annotated_results.csv"
)

# View results with drug information
head(annotated_results[, c("compound", "Score", "name", "moa", "phase")])
}

}
